http.get("/data").subscribe(resp => {
    // 200 
}, err => {
    // 400
    // no connection
});

mock.answer('/foo').with([

]);

// 
// 
const resp$: Obs<any> = client.get("/large_file", { observe: 'events', responseType: 'json' } );
resp$.subscribe(event => {

})

ng g service --http
